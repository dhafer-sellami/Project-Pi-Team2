<form method="post" action="{{ path('app_prise_medicament_delete', {'id': prise_medicament.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prise_medicament.id) }}">
    <button class="btn">Delete</button>
</form>
