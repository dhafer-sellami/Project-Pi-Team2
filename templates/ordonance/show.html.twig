{% extends 'base.html.twig' %}

{% block title %}Ordonance{% endblock %}

{% block body %}
    <h1>Ordonance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordonance.id }}</td>
            </tr>
            <tr>
                <th>Notice</th>
                <td>{{ ordonance.notice }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ ordonance.date ? ordonance.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ordonance_index') }}">back to list</a>

    <a href="{{ path('app_ordonance_edit', {'id': ordonance.id}) }}">edit</a>

    {{ include('ordonance/_delete_form.html.twig') }}
{% endblock %}
