<form method="post" action="{{ path('app_ordonance_delete', {'id': ordonance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordonance.id) }}">
    <button class="btn">Delete</button>
</form>
