<form method="post" action="{{ path('app_admin_rendez_vous_delete', {'id': rendez_vou.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce rendez-vous ?');" style="display:inline;">
   <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendez_vou.id) }}">
   <button type="submit" class="btn btn-danger" style="font-weight: bold;"> <i class="fa fa-trash"></i> Supprimer</button>

</form>